# Resume-Builder
# ResumeCraft — Resume Builder

Features:
- Live preview while editing the form
- Add / remove experience & education sections
- Export resume as JSON
- Export resume to PDF via browser print dialog (print-friendly view)

Run:
Open `index.html` in a browser. For best results use a modern browser (Chrome/Edge/Firefox).
